---
title: "Installation — @sebastienrousseau/commitlint-config"
description: "How to install and set up @sebastienrousseau/commitlint-config."
layout: "doc"
---

# Installation

Install `@sebastienrousseau/commitlint-config` as a development dependency using your preferred package manager.

## npm

```bash
npm install --save-dev @sebastienrousseau/commitlint-config
```

## pnpm

```bash
pnpm add -D @sebastienrousseau/commitlint-config
```

## yarn

```bash
yarn add -D @sebastienrousseau/commitlint-config
```

## bun

```bash
bun add -d @sebastienrousseau/commitlint-config
```

## From Source

```bash
git clone https://github.com/sebastienrousseau/commitlint-config.git
cd commitlint-config
make test
```
